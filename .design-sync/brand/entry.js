// Empty entry for design-sync: this repo is a brand-only (tokens + CSS + fonts)
// import with no JS component library. The converter emits an empty-bodied
// _ds_bundle.js from this, per its tokens-only mode.
export {};
